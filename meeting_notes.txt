# FIGURE 1 EQUIVALENCE 
1. Gamma and Omega are model parameters (A single Gamma. Attribute/Property of the person). It is not ground truth. But for a single model.
NOT FOR DIFFERENT TRIALs. PARAMETERS ARE constants. 
2. Set size is a variable (from trial to trail).
3. Remake the plots for the different set size. The underlying plot will change. 
4. For ours will be Gamma - Log(l)
5. Same figures, four rows - l size. 
6. Still good to draw the 4 horizontal lines in the first row (l=1), keep the first row as it is. 
7. For the other rows, draw l = 1, graphs: horizontal lines all black. 
8. Remove the gamma effective, we have y-axis to read. 
9. l=1, the value of gamma_effective = gamma, so we will be able to see shifts in the colors. 
10. In Bays' model, in his second row, the overlap will be the l=1. 
11. Since we are not using Von Mises and we are not using dedicated subpopulations, are resource divisions is more nuanced.
12. In paul’s model, going over 2nd horizontal line in the first row (l=2 line in first row) will produce the same values (corresponding to colors in the heatmap) of the quantity (quantity = variance, kurtosis) as going over the l=1 line in the second row.
12b. More generally, the values/heatmap-colors over the l-th horizontal line in the first row of plots would match the values/heatmap-colors over the 1st line in the l-th row
14. Q: is this also approximately true for your model?
15. Can we have a metric to measure this concept? = CV?
16. Choose a value to the grid. 
17. Gamma Effective = 1.14730508e+02, make it since it is a grid point.
18. In var_plot, kur_plot and exp_plot = correspondence of the arrays are the same. 
19. These are vectors of values, it should be the same values in Bays. 
20. How are they different? Correlation Coefficient between two vectors. 
21. Average Absolute Error. 



# 29 APRIL:

1. MAKE ALL BLACK COLOR - HORIZONTAL LINES 
2. COLOR RANGE NEEDS TO BE THE SAME - SAME COLOR MEANS THE SAME VALUE ACROSS (VMIN AND VMAX) - ACROSS ROWS IN EACH COLUMN
3. Spike trials in ml_decoder can be optimised by 
4. Make spike_counts a Matrix 
5. Audit -> ml_decoder changed with Prof. (Downstream Changes)
6. CV Table 
7. Final plot in Figure 1 -> l=1 (y = 115Hz, l=2 (y/l)), Right now it is the other way around.